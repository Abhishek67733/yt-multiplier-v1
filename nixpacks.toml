[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["cd multiplier-api && pip install -r requirements.txt"]

[start]
cmd = "cd multiplier-api && uvicorn main:app --host 0.0.0.0 --port $PORT"
